# Fashion Churn Platform - runtime dependencies
#
# Verified against CPython 3.14 on Windows (win_amd64). Notes on the bounds:
#
#   pandas <3.0   - pandas 3.0 makes the string dtype and copy-on-write the default. Those
#                   semantics are not yet well exercised by the scikit-learn / shap versions
#                   below, so we stay on the 2.x line, which has a cp314 wheel (2.3.3).
#   shap >=0.51   - 0.51.0 is the first shap release shipping a cp314 Windows wheel.
#   xgboost /     - both publish version-independent `py3-none-win_amd64` wheels, so they are
#   lightgbm        not tied to the interpreter's ABI tag.

# --- data ---
pandas>=2.2,<3.0
numpy>=2.1,<3

# --- machine learning ---
scikit-learn>=1.6,<2
scipy>=1.14
xgboost>=3.0,<4
lightgbm>=4.5,<5
joblib>=1.4

# --- explainability ---
shap>=0.51,<0.53

# --- dashboard (not implemented yet; pinned so the environment is complete) ---
streamlit>=1.40,<2
plotly>=5.24,<7

# --- configuration ---
python-dotenv>=1.0

# --- testing ---
pytest>=8.3
