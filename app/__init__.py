"""Streamlit application (not implemented yet).

The multi-page dashboard will live here: ``dashboard.py`` as the entry point, with reusable
widgets in ``components/``, Plotly figure builders in ``charts/`` and one module per page in
``pages/``.
"""
